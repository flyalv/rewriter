info:
  name: /health
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url_without_ver}}/health"
  auth: inherit

runtime:
  variables:
    - name: base_url_without_ver
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
